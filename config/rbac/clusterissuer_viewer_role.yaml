# permissions for end users to view clusterissuers.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: clusterissuer-viewer-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: appviewx
    app.kubernetes.io/part-of: appviewx
    app.kubernetes.io/managed-by: kustomize
  name: clusterissuer-viewer-role
rules:
- apiGroups:
  - certm.spark.co.nz
  resources:
  - clusterissuers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - certm.spark.co.nz
  resources:
  - clusterissuers/status
  verbs:
  - get
